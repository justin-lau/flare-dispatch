{
  "name": "@flare-dispatch/gitlab-app",
  "version": "0.0.0",
  "private": true,
  "description": "FlareDispatch GitLab API access — token-authenticated merge-request diff assembly + review-note posting. Deliberately smaller than github-app: no App/install flow, a project access token is the auth.",
  "type": "module",
  "license": "MIT",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "msw": "^2.7.0",
    "typescript": "^5.6.3",
    "vitest": "^3.2.0"
  }
}
